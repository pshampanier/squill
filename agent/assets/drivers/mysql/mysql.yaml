name: mysql
label: MySQL
icon: mysql.svg
description: MySQL is an open-source relational database management system (RDBMS).
capabilities:
  - sql
  - auth_user_password
  - connect_string
  - connect_host
  - connect_socket
defaults:
  PORT: 3306
  USER: root
  CONNECTION_MODE: host
  HOST: localhost
